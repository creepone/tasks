<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>Registration Page</title>
		<meta name="viewport" content="initial-scale=1, maximum-scale=1, user-scalable=no">

	  	<link rel="stylesheet" href="/ios/style/ratchet.css">
	  	<script src="/ios/js/ratchet.js"></script>
		<script src="http://code.jquery.com/jquery-1.9.1.min.js"></script>
		<script>
			$(function() {
				$("a").click(function(evt) {
					if (!$(this).hasClass("disabled") && !$("form").hasClass("submitted")) {
						$("form").addClass("submitted").submit();
					}
					evt.preventDefault();
				});
				
				$("input[name='name']").keyup(function() {
					var val = $(this).val().replace(/\s/g, "");
					$("a").toggleClass("disabled", !val);
				});
			});
		</script>
		<style>
			a.disabled {
				opacity: 0.5;
			}
		</style>
	</head>
	<body>
		<div class="content">
			<div class="content-padded">
				<div class="content-padded">
					We didn't find your user in our records. <br />
					Enter your name to register.
				</div>
				<form method="POST" action="/ios/register">
					<input name="name" type="text" placeholder="Full name" />
					<input name="openid" type="hidden" value="{openid}" />
					<input name="claimedIdentifier" type="hidden" value="{claimedIdentifier}" />
					<input name="device" type="hidden" value="{device}" />
					<a href="#" class="button-main button-block disabled">Register</a>
				</form>
			</div>
		</div>
	</body>
</html>
